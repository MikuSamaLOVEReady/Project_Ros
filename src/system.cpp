#include "../include/system.h"
//#include ""


namespace uwb_slam{

    /*
    void System::Run()
    {
        
        ros->imu->odom
       

    //    std::thread(&,run,);
    
        Uwb_->Serread( *Uwb_);
        std::cout<<Uwb_->x<<std::endl;
    }
    */
}